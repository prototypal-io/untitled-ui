{{yield (hash trigger=(component "ui-dropbutton-trigger" targetId=targetId
                                                         size=size
                                                         states=states
                                                         popup=(hash show=(action "showPopup")
                                                                     hide=(action "hidePopup")
                                                                     toggle=(action "togglePopup")))

              popup=(component "ui-popup" kind="dropbutton"
                                          attachment="top right"
                                          targetAttachment="bottom right"
                                          targetId=targetId
                                          size=size
                                          showPopup=showPopup
                                          show=(action "showPopup")
                                          hide=(action "hidePopup")
                                          toggle=(action "togglePopup")))
}}

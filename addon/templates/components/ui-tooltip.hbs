{{#component frame
  classes=classes
  kind=kind
  tooltip=(hash targetId=targetId
                action=(hash show=(action "show")
                             hide=(action "hide")
                             toggle=(action "toggle"))
                showTooltip=showTooltip)
  size=size as |component|}}
    {{yield component}}
{{/component}}

{{#component frame
  classes=classes
  states=states
  size=size
  ids=(hash
    label=generatedLabelId
    input=generatedInputId)
  type=type as |component|}}
    {{yield component}}
{{/component}}

<button class="ui-button {{sizeClass}}" disabled={{isDisabled}} {{action "onclick"}}>
  {{#component frame
    disabled=isDisabled
    loading=loading
    size=size}}
      {{yield}}
  {{/component}}
</button>

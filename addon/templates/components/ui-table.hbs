{{#component frame
  breakpoints=breakpoints
  activeBreakpoint=activeBreakpoint
  classes=classes
  action=(hash sortBy=(action "sortBy"))
  data=sortedData
  kind=kind
  sortBy=sortBy
  rowLayout=(component "ui-table-layout" activeBreakpoint=activeBreakpoint)
  size=size as |component|}}
    {{yield component}}
{{/component}}
